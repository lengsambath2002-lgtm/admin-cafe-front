---
id: BUG-<AREA>-<###>
title: <one-line summary — if MOBILE, note [iOS]/[Android]>
area: <auth | api | ui | data | integration | infra | mobile | payment>
severity: <blocker | critical | major | minor | trivial>
status: open
reporter: <name>
assignee: <name or empty>
opened: YYYY-MM-DD
resolved: <YYYY-MM-DD when status=resolved/closed/wont-fix>
spec_ref: <docs/specs/... section, or empty>
platform: <iOS | Android | — (only for MOBILE area)>
environment: <DEV | SIT | UAT | PROD>
affects_version: <git tag / app build no., or empty>
fixed_version: <git tag once shipped, or empty>
linked_pr: <MR/PR URL, or empty>
---

# <Bug title>

## Steps to reproduce
1.
2.

## Expected behavior
<what should happen>

## Actual behavior
<what actually happens — error messages, stack traces, screenshots>

## Notes
<additional context: first observed, OS/device, affected users, related bugs>

## Resolution
<filled by Dev/Mobile when status=resolved — root cause + the fix, one paragraph>

## Verification
<filled by QA on close — what was tested, on which env/device, by whom>
