# infra/ — boundary note

DevOps-owned. Dev may read for smoke-verify, but changes go through
`docs/devops/change-requests.md` (Dev never edits infra/ directly).
Secrets live in the deploy provider's settings — never in this repo.
